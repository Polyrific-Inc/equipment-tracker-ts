/**
 * Equipment Tracker TypeScript Application Entry Point
 *
 * This is a placeholder file to test our project setup.
 * We'll build the full application incrementally.
 */

console.log('🚀 Equipment Tracker TS - Starting up...');
console.log('📍 GPS Tracking System Initialized');
console.log('⚙️  Equipment Management Ready');

// Placeholder function to test TypeScript compilation
const welcome = (appName: string): string => {
  return `Welcome to ${appName}!`;
};

// Test our setup
const message = welcome('Equipment Tracker TS');
console.log(message);

// Export for testing
export { welcome };
